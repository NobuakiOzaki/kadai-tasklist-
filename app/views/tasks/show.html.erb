<h1>id: <%= @task.id %> の詳細ページ</h1>

<table class="table table-bordered">
  <tr>
    <th>id</th>
    <td><%= @task.id %></td>
  </tr>
  <tr>
    <th>ステータス</th>
    <td><%= @task.status %></td>
  </tr>
  <tr>
    <th>項目(コンテンツ)</th>
    <td><%= @task.content %></td>
  </tr>
</table>

    <!--<%= link_to "一覧に戻る", tasks_path %>-->
    <!--<%= link_to "タスクリストを変更する", edit_task_path(@task), class: 'btn btn-primary' %>-->
    <!--<%= link_to "タスクリストを削除する", @task, method: :delete, data: { confirm: "本当に削除してよろしいですか？" }, class: 'btn btn-danger' %>-->